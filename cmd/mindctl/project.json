{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mindctl",
  "projectType": "application",
  "sourceRoot": "cmd/mindctl",
  "namedInputs": {
    "internal": ["{workspaceRoot}/internal/**/*"],
    "config": ["{workspaceRoot}/config.example.yaml"]
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/mindctl"],
      "inputs": ["default", "config", "internal"],
      "options": {
        "command": "VERSION=$(tr -d '\\r\\n' < cmd/mindctl/VERSION) && COMMIT=$(git rev-parse --short=7 HEAD) && DATE=$(git show -s --format=%cI HEAD) && mkdir -p dist/apps/mindctl && go build -trimpath -ldflags=\"-s -w -X main.version=$VERSION -X main.commit=$COMMIT -X main.date=$DATE\" -o dist/apps/mindctl/mindctl ./cmd/mindctl"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "inputs": ["default", "config", "internal"],
      "options": { "command": "go test ./cmd/mindctl" }
    },
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Mindctl artifacts are published by the router release workflow.'"
      }
    }
  }
}
